export const selectLocation = (state) => state.rootSlice.items.location;
export const selectForecast = (state) => state.rootSlice.items.forecast;
export const selectCurrent = (state) => state.rootSlice.items.current;
